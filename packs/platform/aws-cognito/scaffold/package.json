{
  "name": "{{project_name}}-auth",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run",
    "test:construct": "vitest run construct",
    "test:guard": "vitest run wiring/typescript"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.263.0",
    "aws-jwt-verify": "^4.0.1",
    "constructs": "^10.8.0",
    "fastify": "^5.11.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@types/node": "^22.19.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
