# {{project_name}}

## What this is

This service was generated by `eep init` from the python-fastapi pack. It provides a five layer FastAPI skeleton (routes, workflow, business process, repository interface, adapter) with a working example feature, structured logging, tracing, and a fully configured lint, type check, and test toolchain, so new business logic can be added right away.

## Setup

Run `make setup` to install dependencies and enable the pre-commit hooks.

## Test

Run `make test` to run the test suite with coverage.

## Verify

Run `make verify` to run the full Engineering Excellence Program verification gate.

## Run

Run `make run` to start the development server with auto reload.
