# Keeps the pinned action SHAs current through reviewed pull requests, which the
# branch protection and CI gate then judge before any bump merges.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  # The uv dependencies are pinned by uv.lock, which dependabot does not support as an update ecosystem yet.
